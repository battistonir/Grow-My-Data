CREATE DATABASE Conservatory;

USE Conservatory;

Create TABLE Users
(
emailAddress varchar(411) NOT NULL,
loginPassword varchar(411) NOT NULL,
theName varchar(411) Not NULL,
CONSTRAINT UniqueSeeds UNIQUE(emailAddress)
);

Create TABLE Plant(
emailAddress varchar(411) NOT NULL,
theTime varChar(411) Not Null,
plantName float NOT NULL,
CONSTRAINT seed UNIQUE(theTime)
);

Create TABLE plantTraits(
emailAddress varchar(411) NOT NULL,
theTime varChar(411) Not Null,
temperature float NOT NULL,
moisture float NOT NULL,
humidity float NOT NULL,
sunlight float NOT NULL
);

ALTER TABLE Garden ALTER COLUMN; 

DROP TABLE Garden;

DROP DATABASE Conservatory;

ALTER TABLE Garden ADD UNIQUE()

DROP CONSTRAINT UniqueSeeds;

ALTER TABLE Garden ADD;

ALTER TABLE Garden DROP COLUMN;

TRUNCATE TABLE Garden;

sp_rename 'Conservatory', '';

sp_rename 'Conservatory.Garden','';

ADD CHECK (='');

UPDATE
Where
DELETE From

SELECT * From Plant Order by StudentName ASC

SELECT FROM GROUP BY CUBE() ORDER BY;

SELECT FROM GROUP BY ROLLUP();

SELECT * FROM 

